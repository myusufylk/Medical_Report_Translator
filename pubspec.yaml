name: medikal_rapor_tercuman
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # Temel Paketler
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  file_picker: ^8.1.2
  google_generative_ai: ^0.4.6
  flutter_dotenv: ^5.1.0
  flutter_markdown: ^0.7.3+1
  
  # API ve Dosya Yönetimi (Önceki hataları önlemek için eklendi)
  http: ^1.2.1
  http_parser: ^4.0.0
  path_provider: ^2.1.3

  # Yeni Eklenen Özellikler (TTS ve PDF)
  flutter_tts: ^4.2.2
  pdf: ^3.11.1
  printing: ^5.13.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env